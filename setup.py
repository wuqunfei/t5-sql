from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A SQL dialects transformer for PL/PG SQL, TSQL',
    author='Qunfei Wu',
    license='',
)
